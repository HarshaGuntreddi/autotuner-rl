FROM ubuntu:22.04
RUN apt-get update && apt-get install -y \
    build-essential cmake git python3 python3-pip \
    ocl-icd-opencl-dev pocl-opencl-icd && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY . /app
RUN pip3 install -e .[dev]
CMD ["bash"]
